/* Local Production Project Studio — evidence checklist, self-review rubric, portfolio artifact builder. */
(function(){
const projectsView=document.getElementById('projects');projectsView.innerHTML=`<div class="hero"><div><h1>Production Project Studio</h1><p>Turn completed analysis into credible portfolio evidence. Every project follows professional delivery standards.</p></div><button class="primary" onclick="openProjectStudio('sales')">Start a project →</button></div><div class="grid project-grid" id="studioProjects"></div><div id="projectWorkspace" class="card pad" style="display:none;margin-top:18px"></div><div id="projectReview" class="card pad" style="display:none;margin-top:18px"></div>`;
const templates=[
{id:'sales',icon:'🛍',name:'Sales Performance Dashboard',level:'BEGINNER',tools:'Excel / Google Sheets / Power BI',brief:'The Sales Director needs a Q2 performance review and a recommendation for Q3 support allocation.',data:'Northstar Retail sales dataset',skills:['Cleaning','KPIs','Pivots','Storytelling']},
{id:'campaign',icon:'📣',name:'Campaign Experiment Memo',level:'INTERMEDIATE',tools:'Excel / SQL / Statistics',brief:'Marketing tested new creative. Determine whether to roll out, hold, or iterate using effect size, uncertainty, and guardrails.',data:'Campaign experiment dataset',skills:['A/B testing','Statistics','Executive memo']},
{id:'retention',icon:'☁',name:'SaaS Retention Investigation',level:'INTERMEDIATE',tools:'SQL / BI',brief:'The VP of Customer Success needs the segments behind a paid-account retention decline and a measurable action plan.',data:'SaaS accounts, events, subscriptions, tickets',skills:['SQL','Cohorts','Segmentation']},
{id:'segmentation',icon:'🐍',name:'Customer Value & Segmentation',level:'ADVANCED',tools:'Python / Pandas / Visualization',brief:'The Growth Lead needs customer segments that can guide retention or expansion action.',data:'Customer and transaction dataset',skills:['Python','Features','Visualization']},
{id:'profit',icon:'💳',name:'Retail Profitability Review',level:'ADVANCED',tools:'SQL / Excel / BI',brief:'Revenue is growing but profit is falling. Identify margin drivers and propose a controlled intervention.',data:'P&L, discounts, products, returns',skills:['Finance','Root cause','Dashboard']},
{id:'supply',icon:'🚚',name:'Supply Chain Delay Diagnosis',level:'PROFESSIONAL',tools:'SQL / Python / BI',brief:'On-time delivery dropped. Separate warehouse, inventory, and courier drivers before recommending a response.',data:'Orders, inventory, delivery events, routes',skills:['Operations','SQL','Case analysis']}
];
function completed(){try{return JSON.parse(localStorage.getItem('studioCompleted')||'[]')}catch{return []}}function save(x){localStorage.setItem('studioCompleted',JSON.stringify(x))}
function cards(){let c=completed();document.getElementById('studioProjects').innerHTML=templates.map(t=>`<div class="card project"><div class="project-art">${t.icon}</div><div class="project-body"><span class="pill ${t.level==='BEGINNER'?'beginner':t.level==='INTERMEDIATE'?'intermediate':t.level==='ADVANCED'?'advanced':'pro'}">${t.level}</span><h3>${t.name}</h3><p>${t.brief}</p><div class="tags">${t.skills.map(s=>`<span class="tag">${s}</span>`).join('')}</div><button class="secondary" style="margin-top:14px" onclick="openProjectStudio('${t.id}')">${c.includes(t.id)?'Review artifact →':'Open brief →'}</button></div></div>`).join('')}
let active;window.openProjectStudio=function(id){active=templates.find(x=>x.id===id);document.getElementById('projectReview').style.display='none';let p=localStorage.getItem('studioDraft_'+id),draft={};try{draft=JSON.parse(p||'{}')}catch{}let checks=['Business brief & stakeholder','Dataset, grain & data dictionary','Cleaning and validation log','Analysis / queries / notebook','Metrics and definitions','Dashboard or visual evidence','Insight and recommendation','Executive summary','README / reproducibility','Interview walkthrough'];document.getElementById('projectWorkspace').style.display='block';document.getElementById('projectWorkspace').innerHTML=`<div class="title-row"><div><span class="pill intermediate">${active.level}</span><h2 style="margin:12px 0 0">${active.name}</h2><p style="color:var(--muted);margin:6px 0">${active.tools}</p></div><span class="tag">${active.data}</span></div><div class="callout"><b>Stakeholder brief:</b> ${active.brief}</div><div class="grid two"><div><h3 style="font-size:14px">Evidence checklist</h3>${checks.map((x,i)=>`<label style="display:block;padding:7px 0;border-bottom:1px solid var(--line);font-size:13px"><input type="checkbox" class="projectCheck" data-i="${i}" ${draft.checks?.includes(i)?'checked':''}> ${x}</label>`).join('')}</div><div><h3 style="font-size:14px">Project delivery notes</h3><label style="font-size:12px;font-weight:700">Artifact link / local file reference<input id="artifactRef" value="${draft.artifact||''}" placeholder="e.g. /projects/sales-dashboard.xlsx or GitHub URL" style="display:block;width:100%;padding:10px;margin-top:6px;border:1px solid var(--line);border-radius:7px"></label><label style="font-size:12px;font-weight:700;display:block;margin-top:11px">Executive summary (100–180 words)<textarea id="executiveSummary" style="width:100%;height:150px;padding:10px;margin-top:6px;border:1px solid var(--line);border-radius:7px;resize:vertical" placeholder="Decision, evidence, recommendation, metric, guardrail, limitation…">${draft.summary||''}</textarea></label><label style="font-size:12px;font-weight:700;display:block;margin-top:11px">Interview walkthrough notes<textarea id="interviewWalkthrough" style="width:100%;height:90px;padding:10px;margin-top:6px;border:1px solid var(--line);border-radius:7px;resize:vertical" placeholder="How you framed it, validated data, analyzed, and made your recommendation…">${draft.walkthrough||''}</textarea></label></div></div><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:18px"><button class="secondary" onclick="saveProjectDraft()">Save local draft</button><button class="primary" onclick="reviewProjectSubmission()">Evaluate project →</button></div>`;document.getElementById('projectWorkspace').scrollIntoView({behavior:'smooth'})};
window.saveProjectDraft=function(){let checks=[...document.querySelectorAll('.projectCheck:checked')].map(x=>+x.dataset.i),d={checks,artifact:document.getElementById('artifactRef').value.trim(),summary:document.getElementById('executiveSummary').value.trim(),walkthrough:document.getElementById('interviewWalkthrough').value.trim(),updatedAt:new Date().toISOString()};localStorage.setItem('studioDraft_'+active.id,JSON.stringify(d));toast('Project draft saved locally.')};window.reviewProjectSubmission=function(){saveProjectDraft();let d=JSON.parse(localStorage.getItem('studioDraft_'+active.id)),checks=d.checks.length,summary=d.summary,walk=d.walkthrough;let framing=checks>=2?85:35,quality=checks>=4?80:checks*18,technical=checks>=6?75:checks*12,communication=Math.min(100,Math.round((summary.length/4+walk.length/5)/2)),recommend=/(recommend|should|action)/i.test(summary)&&/(metric|monitor|measure)/i.test(summary)&&/(limit|risk|uncertain)/i.test(summary)?85:45,overall=Math.round(framing*.1+quality*.2+technical*.25+recommend*.2+communication*.15+(checks>=9?10:0));let r=document.getElementById('projectReview');r.style.display='block';r.innerHTML=`<div class="title-row"><div><span class="pill beginner">LOCAL RUBRIC REVIEW</span><h2 style="margin:12px 0 0">${active.name} — ${overall}/100</h2></div><span class="tag">${checks}/10 evidence items</span></div><div class="grid two"><div><h3 style="font-size:14px">Scorecard</h3><table class="table"><tr><td>Business framing</td><td>${framing}/100</td></tr><tr><td>Data quality & documentation</td><td>${quality}/100</td></tr><tr><td>Technical evidence</td><td>${technical}/100</td></tr><tr><td>Recommendation quality</td><td>${recommend}/100</td></tr><tr><td>Communication</td><td>${communication}/100</td></tr></table></div><div><h3 style="font-size:14px">Next improvements</h3><p style="color:var(--muted);line-height:1.6">${checks<10?'Complete missing evidence items before calling this portfolio-ready. ':''}${summary.length<400?'Strengthen the summary with a specific decision, observed evidence, action owner, metric, guardrail, and limitation. ':'Your written summary has enough detail for a review. '}${!d.artifact?'Add a local file reference or public artifact link so an interviewer can inspect the work. ':''}</p><button class="primary" onclick="finalizeProject(${overall})">${overall>=70?'Mark portfolio-ready →':'Keep improving draft'}</button></div></div>`;r.scrollIntoView({behavior:'smooth'})};window.finalizeProject=function(score){if(score<70){toast('Project remains in draft. Improve the missing evidence and re-evaluate.');return}let c=completed();if(!c.includes(active.id)){c.push(active.id);save(c);localStorage.setItem('spProjects',String(Math.max(+localStorage.getItem('spProjects')||0,c.length)));}let d=JSON.parse(localStorage.getItem('studioDraft_'+active.id));d.score=score;d.portfolioReady=true;localStorage.setItem('studioDraft_'+active.id,JSON.stringify(d));cards();toast('Portfolio-ready project evidence saved locally. +100 XP.');};const oldGo=window.go;window.go=function(id){oldGo(id);if(id==='projects')cards()};cards();})();
