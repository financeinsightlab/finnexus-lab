/* Local Notes and Knowledge Workspace with tags, linked entities, snippets and backup-friendly persistence. */
(function(){
const main=document.querySelector('.main'),sec=document.createElement('section');sec.id='notes';sec.className='view';sec.innerHTML=`<div class="hero"><div><h1>Notes & Knowledge Workspace</h1><p>Capture formulas, concepts, code patterns, interview answers, project decisions, and personal reflections in one local searchable notebook.</p></div><button class="secondary" onclick="go('vault')">Open Local Vault</button></div><div id="notesContent"></div>`;main.appendChild(sec);
let nav=document.getElementById('nav'),a=document.createElement('a');a.href='#notes';a.dataset.view='notes';a.innerHTML='<span class="ico">✎</span><span>Notes Workspace</span>';let vault=[...nav.querySelectorAll('a')].find(x=>x.dataset.view==='vault');nav.insertBefore(a,vault);a.onclick=e=>{e.preventDefault();go('notes');renderNotes()};
function get(){try{return JSON.parse(localStorage.getItem('knowledgeNotes')||'[]')}catch{return []}}function save(x){localStorage.setItem('knowledgeNotes',JSON.stringify(x))}function esc(x){return x.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function fmt(x){return esc(x).replace(/```([\s\S]*?)```/g,'<pre class="example" style="white-space:pre-wrap">$1</pre>').replace(/^### (.*)$/gm,'<h3>$1</h3>').replace(/^## (.*)$/gm,'<h2>$1</h2>').replace(/^# (.*)$/gm,'<h1 style="font-size:20px">$1</h1>').replace(/\*\*(.*?)\*\*/g,'<b>$1</b>').replace(/`([^`]+)`/g,'<code>$1</code>').replace(/\n/g,'<br>')}
function tags(n){return [...new Set(n.flatMap(x=>x.tags||[]))].sort()}
window.renderNotes=function(){let n=get(),t=tags(n);document.getElementById('notesContent').innerHTML=`<div class="grid dashboard"><div class="card pad"><div class="title-row"><div><h2>New note</h2><p style="color:var(--muted);margin:5px 0">Use simple Markdown: headings, bold text, inline code, and code blocks.</p></div><button class="secondary" onclick="previewKnowledgeNote()">Preview</button></div><input id="noteTitle" placeholder="Title, e.g. SQL JOIN validation checklist" style="width:100%;padding:10px;border:1px solid var(--line);border-radius:7px;margin-bottom:10px"><div class="grid two"><select id="noteEntity" style="padding:10px;border:1px solid var(--line);border-radius:7px"><option value="General">Link: General</option><option value="Lesson">Link: Lesson</option><option value="Project">Link: Project</option><option value="Dataset">Link: Dataset</option><option value="Case">Link: Case</option><option value="Metric">Link: Metric</option><option value="Interview">Link: Interview</option></select><input id="noteTags" placeholder="Tags, comma-separated" style="padding:10px;border:1px solid var(--line);border-radius:7px"></div><textarea id="noteBody" style="width:100%;height:220px;padding:11px;border:1px solid var(--line);border-radius:7px;margin-top:10px;resize:vertical" placeholder="Write your note, formula, SQL pattern, Python snippet, interview answer or project decision…"></textarea><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:10px"><button class="secondary" onclick="insertNoteTemplate('sql')">SQL template</button><button class="secondary" onclick="insertNoteTemplate('reflection')">Reflection template</button><button class="primary" onclick="saveKnowledgeNote()">Save local note</button></div><div id="notePreview" style="display:none;margin-top:14px" class="callout"></div></div><div class="card pad"><div class="title-row"><h2>Knowledge signals</h2><span class="pill beginner">LOCAL ONLY</span></div><div class="next-task"><div class="task-icon">✎</div><div><b>${n.length} saved notes</b><p>Formulas, snippets, reflections, project evidence and interview practice.</p></div></div><div class="next-task"><div class="task-icon">#</div><div><b>${t.length} tags</b><p>${t.slice(0,8).join(' · ')||'Add tags to organize your knowledge.'}</p></div></div><div class="next-task"><div class="task-icon">▣</div><div><b>Backup included</b><p>Knowledge notes are included in Local Vault backup after saving.</p></div></div><button class="secondary" style="margin-top:12px" onclick="exportKnowledgeNotes()">Export notes as JSON ↓</button></div></div><div class="card pad" style="margin-top:18px"><div class="title-row"><h2>Search your knowledge base</h2><div style="display:flex;gap:8px"><input id="knowledgeSearch" placeholder="Search title, text, tag…" style="padding:9px;border:1px solid var(--line);border-radius:7px" oninput="filterKnowledgeNotes()"><select id="knowledgeFilter" style="padding:9px;border:1px solid var(--line);border-radius:7px" onchange="filterKnowledgeNotes()"><option value="all">All links</option><option>General</option><option>Lesson</option><option>Project</option><option>Dataset</option><option>Case</option><option>Metric</option><option>Interview</option></select></div></div><div id="notesList"></div></div>`;filterKnowledgeNotes()};
window.filterKnowledgeNotes=function(){let n=get(),q=(document.getElementById('knowledgeSearch')?.value||'').toLowerCase(),f=document.getElementById('knowledgeFilter')?.value||'all';let x=n.filter(a=>(f==='all'||a.entity===f)&&JSON.stringify(a).toLowerCase().includes(q));document.getElementById('notesList').innerHTML=x.length?x.slice().reverse().map(a=>`<div class="next-task" style="align-items:flex-start"><div class="task-icon">${a.entity==='Project'?'▣':a.entity==='Metric'?'#':'✎'}</div><div style="flex:1"><div style="display:flex;gap:7px;align-items:center;flex-wrap:wrap"><b>${esc(a.title)}</b><span class="tag">${a.entity}</span>${(a.tags||[]).map(t=>`<span class="tag">#${esc(t)}</span>`).join('')}</div><p style="margin-top:6px">${esc(a.body.slice(0,220))}${a.body.length>220?'…':''}</p><small style="color:var(--muted)">${new Date(a.at).toLocaleString()}</small></div><button class="secondary" onclick="openKnowledgeNote('${a.id}')">Open</button><button class="secondary" style="color:var(--red)" onclick="deleteKnowledgeNote('${a.id}')">Delete</button></div>`).join(''):'<div class="empty"><h3>No notes found</h3><p>Write your first formula, insight, or interview answer above.</p></div>'};
window.previewKnowledgeNote=function(){let b=document.getElementById('noteBody').value,e=document.getElementById('notePreview');e.style.display='block';e.innerHTML=fmt(b||'Write a note to preview it here.')};window.insertNoteTemplate=function(k){let e=document.getElementById('noteBody');if(k==='sql')e.value+='\n## SQL validation checklist\n- Grain: \n- Population filter: \n- Join cardinality: \n- Row count / distinct key check: \n- Reconciliation: \n```sql\nSELECT\n  -- write query\n```\n';else e.value+='\n## Reflection\n**What I learned:** \n**What was difficult:** \n**Business use:** \n**Next action:** \n';e.focus()};
window.saveKnowledgeNote=function(){let title=document.getElementById('noteTitle').value.trim(),body=document.getElementById('noteBody').value.trim();if(!title||!body){toast('Add both a title and note body.');return}let n=get();n.push({id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),title,body,entity:document.getElementById('noteEntity').value,tags:document.getElementById('noteTags').value.split(',').map(x=>x.trim().toLowerCase()).filter(Boolean),at:new Date().toISOString()});save(n);document.getElementById('noteTitle').value='';document.getElementById('noteBody').value='';document.getElementById('noteTags').value='';toast('Note saved locally and included in your workspace.');renderNotes()};
window.openKnowledgeNote=function(id){let a=get().find(x=>x.id===id);if(!a)return;document.getElementById('noteTitle').value=a.title;document.getElementById('noteEntity').value=a.entity;document.getElementById('noteTags').value=(a.tags||[]).join(', ');document.getElementById('noteBody').value=a.body;window.scrollTo({top:0,behavior:'smooth'});toast('Note loaded. Save a new version after editing.')};window.deleteKnowledgeNote=function(id){if(!confirm('Delete this local note?'))return;save(get().filter(x=>x.id!==id));renderNotes();toast('Note deleted.')};window.exportKnowledgeNotes=function(){let blob=new Blob([JSON.stringify({format:'signalpath-knowledge-notes',notes:get()},null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='signalpath-notes.json';a.click();URL.revokeObjectURL(url);toast('Notes exported. Keep your backup private.')};
const oldGo=window.go;window.go=function(id){oldGo(id);if(id==='notes')renderNotes()};renderNotes();})();
